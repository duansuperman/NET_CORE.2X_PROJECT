@model IEnumerable<QuanLySinhVien.Models.Khoa>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenKhoa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenKhoa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-maKhoa="@item.MaKhoa">Edit</a> |
                <a asp-action="Details" asp-route-maKhoa="@item.MaKhoa">Details</a> |
                <a asp-action="Delete" asp-route-maKhoa="@item.MaKhoa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
