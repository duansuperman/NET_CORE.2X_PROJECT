@model IEnumerable<QuanLySinhVien.Models.MonHoc>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenMon)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenMon)
            </td>
            <td>
                <a asp-action="Edit" asp-route-maMon="@item.MaMon">Edit</a> |
                <a asp-action="Details" asp-route-maMon="@item.MaMon">Details</a> |
                <a asp-action="Delete" asp-route-maMon="@item.MaMon">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
